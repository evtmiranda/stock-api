swagger: "2.0"
info:
  version: 1.0.2
  title: Stock API
  description: A simple API to manage stock
schemes:
  - https
  - http
host: fit-stock-api.herokuapp.com
basePath: /api/v1
tags:
- name: "user"
  description: "Operations about user"
- name: "profile"
  description: "Operations about profile"
- name: "module"
  description: "Operations about module"
- name: "stock"
  description: "Operations about stock"
paths:
  /user:
    get:
      tags:
        - "user"
      summary: Get all users
      description: Returns a list containing all users.
      consumes:
      - "application/json"
      responses:
        200:
          description: A list of User
          schema:
            $ref: "#/definitions/User"
    post:
      tags:
      - "user"
      summary: "Create a new user"
      description: "Create a new unique user."
      consumes:
      - "application/json"
      produces:
      - "application/json"
      parameters:
      - in: "body"
        name: "body"
        description: "User information"
        required: true
        schema:
          $ref: "#/definitions/UserWithoutId"
      responses:
        201:
          description: "successful operation"
          schema:
            $ref: "#/definitions/CreatedUser"
  /user/{id}:
    put:
      tags:
      - "user"
      summary: "Update an user"
      description: "Update an user."
      consumes:
      - "application/json"
      produces:
      - "application/json"
      parameters:
      - name: "id" 
        in: "path"
        description: "user id"
        required: true
        type: "string"
      - in: "body"
        name: "body"
        description: "Updated user object"
        required: true
        schema:
          $ref: "#/definitions/CreatedUser"
      responses:
        200:
          description: "successful operation"
          schema:
            $ref: "#/definitions/CreatedUser"
    delete:
      tags:
      - "user"
      summary: "Delete an user"
      description: "Delete an user."
      parameters:
      - name: "id"
        in: "path"
        description: "user id"
        required: true
        type: "string"
      responses:
        200:
          description: "successful operation"
  /profile:
    get:
      tags:
        - "profile"
      summary: Get all profiles
      description: Returns a list containing all profiles.
      consumes:
      - "application/json"
      responses:
        200:
          description: A list of Profiles
          schema:
            $ref: "#/definitions/Profile"
  /module:
    get:
      tags:
        - "module"
      summary: Get all modules
      description: Returns a list containing all modules.
      consumes:
      - "application/json"
      responses:
        200:
          description: A list of Modules
          schema:
            $ref: "#/definitions/Module"
  /stock:
    get:
      tags:
        - "stock"
      summary: Get all stocks
      description: Returns a list containing all stocks.
      consumes:
      - "application/json"
      responses:
        200:
          description: A list of Stocks
          schema:
            $ref: "#/definitions/Stock"
  /stock/{id}:
    delete:
      tags:
      - "stock"
      summary: "Delete a stock"
      description: "Delete a stock."
      parameters:
      - name: "id"
        in: "path"
        description: "stock id"
        required: true
        type: "string"
      responses:
        200:
          description: "successful operation"
definitions:
  User:
    type: "object"
    properties:
      id:
        type: "integer"
        format: "int16"
      name:
        type: "string"
      username:
        type: "string"
      password:
        type: "string"
      profileId:
        type: "integer"
        format: "int16"
      createdAt:
        type: "string"
        format: "date-time"
      updatedAt:
        type: "string"
        format: "date-time"
      deletedAt:
        type: "string"
        format: "date-time"
    xml:
      name: "User"
  CreatedUser:
    type: "object"
    properties:
      id:
        type: "integer"
        format: "int16"
      name:
        type: "string"
        example: "Admin"
      username:
        type: "string"
        example: "admin"
      password:
        type: "string"
        example: "123456"
      profileId:
        type: "integer"
        example: 1
        format: "int16"
      createdAt:
        type: "string"
        format: "date-time"
      updatedAt:
        type: "string"
        format: "date-time"
      deletedAt:
        type: "string"
        format: "date-time"
  UserWithoutId:
    type: "object"
    properties:
      name:
        type: "string"
      username:
        type: "string"
      password:
        type: "string"
      profileId:
        type: "integer"
        format: "int16"
      createdAt:
        type: "string"
        format: "date-time"
      updatedAt:
        type: "string"
        format: "date-time"
      deletedAt:
        type: "string"
        format: "date-time"
    xml:
      name: "UserWithoutId"
  Profile:
    type: "object"
    properties:
      id:
        type: "integer"
        format: "int16"
        example: 1
      name:
        type: "string"
        example: "Admin"
      description:
        type: "string"
        example: "Usuário com todas as permissões possíveis."
      createdAt:
        type: "string"
        format: "date-time"
      updatedAt:
        type: "string"
        format: "date-time"
      deletedAt:
        type: "string"
        format: "date-time"
    xml:
      name: "Profile"
  Module:
    type: "object"
    properties:
      id:
        type: "integer"
        format: "int16"
        example: 1
      name:
        type: "string"
        example: "home"
      createdAt:
        type: "string"
        format: "date-time"
      updatedAt:
        type: "string"
        format: "date-time"
      deletedAt:
        type: "string"
        format: "date-time"
    xml:
      name: "Module"
  Stock:
    type: "object"
    properties:
      id:
        type: "integer"
        format: "int16"
        example: 1
      lot:
        type: "string"
        example: "lot 1"
      description:
        type: "string"
      reference:
        type: "string"
      quantity:
        type: "integer"
        format: "int16"
        example: 500
      entryDate:
        type: "string"
        format: "date-time"
      tag:
        type: "boolean"
      store:
        type: "string"
      unitValue:
        type: "number"
        format: "float"
      outputDate:
        type: "string"
        format: "date-time"
      outputQuantity:
        type: "integer"
        format: "int16"
        example: 450
      createdAt:
        type: "string"
        format: "date-time"
      paymentDate:
        type: "string"
        format: "date-time"
      paymentAmount:
        type: "number"
        format: "float"
      updatedAt:
        type: "string"
        format: "date-time"
      deletedAt:
        type: "string"
        format: "date-time"
    xml:
      name: "Stock"